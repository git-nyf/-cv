# 对话整理与项目执行记录

更新时间：2026-06-07  
工作目录：`D:\AAA中药cv`

> 说明：本文档整理当前可见对话、上下文压缩摘要和本地工程状态，目标是支持后续继续执行项目。它不是逐字聊天记录，而是包含需求、决策、已完成工作、风险和下一步动作的完整执行纪要。

## 1. 用户总体目标

用户要求围绕“基于 YOLOv12 的中药饮片智能识别与分类系统”完成课程项目交付，结合工作区中的需求分析报告、概要设计 PPT 和参考论文，从零完成环境搭建、数据集处理、模型训练、参数调优、推理部署、多维度性能测试、报告与概要设计汇报 PPT。

用户明确要求：

- 默认使用简体中文沟通和撰写文档。
- 不伪造训练结果或模型指标。
- 若没有真实 bbox 标注，不能声称已经完成真实检测框标注。
- 如果数据只有分类目录，可以采用第二方案进行分类训练；必要时可保留“整图框 YOLO 检测”作为工程链路演示，但必须说明其局限。
- 当前补完整的数据集位于 `D:\AAA中药cv\data`，要求使用这些数据进行训练。
- 在继续训练前，先把对话和项目状态整理到工作目录文档中。
- 增加 StopHook：只有当 agent 输出 `我绝对确定所有任务都已经完成且所有目标都已经达成` 时才允许停止。

## 2. 已使用或应使用的技能

- `senior-computer-vision`：用于判断目标检测、分类训练、bbox 标注、YOLO 数据格式、评估和部署策略。
- `planning-with-files`：用于把复杂任务的计划、发现、进度保存为工作区文件。
- 前序上下文中还曾使用或参考过 `docx`、`pdf`、`pptx`、`academic-pptx`、`webapp-testing` 等能力，用于读取需求文档、论文、生成报告和 PPT、验证前后端。

## 3. 已有工程状态

工程目录：

`D:\AAA中药cv\yolo12_tcm_project`

主要内容包括：

- `configs/`：训练和数据配置。
- `data/`：demo 数据、清洗数据、临时转换数据。
- `scripts/`：环境、数据处理、训练、评估、推理、导出、benchmark、QA、报告脚本。
- `backend/`：FastAPI 后端。
- `frontend/`：Vue3 + Vite 前端。
- `runs/`：训练、评估、推理输出。
- `reports/`：测试报告、PPT、QA 报告和图像。
- `docs/`：环境、数据、模型、API 文档。
- `tests/`：单元测试。

已存在关键脚本：

- `scripts/train_yolo12.py`
- `scripts/train_yolo12_cls.py`
- `scripts/evaluate_yolo12.py`
- `scripts/evaluate_yolo12_cls.py`
- `scripts/inspect_classification_dataset.py`
- `scripts/prepare_classification_dataset.py`
- `scripts/classification_to_fullbox_yolo.py`
- `scripts/stop_guard.py`

已存在模型文件：

- `D:\AAA中药cv\yolo12_tcm_project\yolo12n.pt`
- `D:\AAA中药cv\yolo12_tcm_project\yolo11n-cls.pt`

## 4. 前序已完成工作

前序上下文显示，项目已经完成过一轮工程搭建与 demo 验证：

- 环境：已创建 `.venv` 并安装核心依赖。
- 数据处理：支持 YOLO 格式校验、划分、分类目录转整图框 YOLO 数据。
- 训练：支持 YOLOv12 检测训练与分类训练入口。
- 评估：支持 Ultralytics 指标输出。
- 推理：支持图片、视频和摄像头相关脚本。
- 导出：支持 ONNX 等模型导出入口。
- 后端：FastAPI 提供 `/health`、`/models`、`/detect/image` 等接口。
- 前端：Vue3 页面支持图片上传、摄像头页签、训练入口、模型选择和结果展示。
- QA：已有工程自检、前端 QA、后端 QA 脚本。
- 报告：已生成 Markdown/DOCX 测试报告和概要设计 PPT。

前序 demo 检测训练只用于跑通工程链路，不代表真实中药饮片性能。

## 5. 前序部分真实数据训练

用户先前只提供了部分数据，类别包括 `丝瓜络` 和 `九香虫`，没有 bbox 标注。当时采用“分类目录转整图框 YOLO”的临时方案进行了部分检测训练。

前序输出路径：

- 数据：`D:\AAA中药cv\yolo12_tcm_project\data\partial_fullbox_yolo`
- 训练：`D:\AAA中药cv\yolo12_tcm_project\runs\train\partial_fullbox_yolo12n_cpu`
- 报告：`D:\AAA中药cv\yolo12_tcm_project\reports\partial_training_report.md`

前序指标很低且基于整图框伪标注，只能作为工程记录，不能作为正式检测能力证明。

## 6. 当前完整数据检查结论

当前用户表示数据集已经补完整，位置为：

`D:\AAA中药cv\data`

初步扫描结果：

- `.jpg` 图片：9614 张。
- `.cache` 文件：2 个。
- `.yaml` 文件：1 个。
- 未发现 `.txt`、`.xml`、`.json`、`.csv` 等 bbox 标注文件。
- 数据结构为：
  - `data/train/<类别名>/*.jpg`
  - `data/val/<类别名>/*.jpg`
- `data/data.yaml` 的 `path` 仍是占位文本，并且类别名称在终端显示为乱码，不适合作为当前训练的可靠配置来源。

判断：

- 该数据集是完整的分类数据集，不是标准 YOLO 目标检测数据集。
- 不能凭空补出真实 bbox 标注，因为真实 bbox 需要人工标注、半自动标注后复核，或来自已有标注文件。
- 本轮应优先执行分类训练，这是与当前数据形态匹配、指标更可信的方案。
- 若课程材料必须展示 YOLO 检测链路，可额外生成整图框 YOLO 数据，但只能标为弱标注/流程演示。

## 7. StopHook 当前状态

已发现 `.claude/settings.local.json` 中配置了 Stop hook：

```json
{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"D:\\\\AAA中药cv\\\\yolo12_tcm_project\\\\.venv\\\\Scripts\\\\python.exe\" \"D:\\\\AAA中药cv\\\\yolo12_tcm_project\\\\scripts\\\\stop_guard.py\""
          }
        ]
      }
    ]
  }
}
```

`scripts/stop_guard.py` 默认要求短语：

`我绝对确定所有任务都已经完成且所有目标都已经达成`

脚本逻辑：

- 从 stdin、JSON payload、transcript path 和显式文本中收集输出内容。
- 若检测到指定短语，返回 0 并允许停止。
- 若没有检测到指定短语，返回 2 并输出阻止信息。

注意：这是项目级 hook 配置与守门脚本。是否由当前 Codex 桌面客户端实际执行，取决于客户端是否读取 `.claude/settings.local.json` 这类 hook 配置。本轮会测试脚本本身，不会伪称已经接管客户端底层停止机制。

## 8. 本轮执行计划

1. 创建/更新工作区计划文件：`task_plan.md`、`findings.md`、`progress.md`。
2. 更新本文档为 UTF-8 中文版本。
3. 测试 `stop_guard.py`：无短语应阻止，有短语应放行。
4. 完整审计 `D:\AAA中药cv\data`：类别数、训练/验证数量、类别不一致、坏图。
5. 生成或确认分类训练数据目录。
6. 执行短轮次 YOLOv12/Ultralytics 分类训练。
7. 评估训练结果，保存权重、指标和报告。
8. 更新项目报告，明确“无法自动生成真实 bbox”的原因和后续人工标注建议。

## 9. 本轮完成结果

已完成工作：

- 已创建并更新：
  - `D:\AAA中药cv\task_plan.md`
  - `D:\AAA中药cv\findings.md`
  - `D:\AAA中药cv\progress.md`
  - `D:\AAA中药cv\对话整理_项目执行记录.md`
- 已核验 StopHook：
  - 配置文件：`D:\AAA中药cv\.claude\settings.local.json`
  - 守门脚本：`D:\AAA中药cv\yolo12_tcm_project\scripts\stop_guard.py`
  - 结果：无完成短语时阻止；最后一条 assistant 输出包含完成短语时放行；仅用户消息或历史内容包含短语时不放行。
- 已完成数据审计：
  - 原始数据：93 类，train 7847 张，val 1767 张，总计 9614 张。
  - `大腹皮` 只出现在 val 中，没有训练样本。
  - 未发现真实 bbox 标注文件，因此不能自动补真实 YOLO 检测框。
- 已生成清洗后的分类训练集：
  - `D:\AAA中药cv\yolo12_tcm_project\data\classification_clean`
  - 92 类，train 7847 张，val 1748 张，总计 9595 张。
- 已完成严格图片内容审计：
  - `D:\AAA中药cv\yolo12_tcm_project\reports\full_classification_image_content_audit.md`
  - 发现 150 个内容层面问题，包括扩展名不匹配、GIF 伪装 JPG、图片流损坏。
- 已完成 YOLOv12n 分类部分训练：
  - 模型：`yolo12n-cls.yaml`
  - 设备：CPU
  - epoch：1
  - 输出：`D:\AAA中药cv\yolo12_tcm_project\runs\train\full_classify_yolo12n_cls_cpu_smoke`
  - 权重：
    - `D:\AAA中药cv\yolo12_tcm_project\runs\train\full_classify_yolo12n_cls_cpu_smoke\weights\best.pt`
    - `D:\AAA中药cv\yolo12_tcm_project\runs\train\full_classify_yolo12n_cls_cpu_smoke\weights\last.pt`
- 已完成独立验证：
  - Top-1：0.0476737507
  - Top-5：0.1958644390
  - Fitness：0.1217690948
- 已生成训练报告：
  - `D:\AAA中药cv\yolo12_tcm_project\reports\full_classification_training_report.md`
- 已新增正式训练配置：
  - `D:\AAA中药cv\yolo12_tcm_project\configs\train_cls_full_cpu.yaml`
  - `D:\AAA中药cv\yolo12_tcm_project\configs\train_cls_full_gpu.yaml`

限制说明：

- 本机 CUDA 不可用，训练在 CPU 上完成。
- 本地没有 `yolo12n-cls.pt` 预训练分类权重，因此本轮使用 YOLOv12n 分类结构从头训练 1 epoch。
- 当前指标只代表完整数据处理和训练链路已经跑通，不能代表正式最终准确率。
- 若要做真实 YOLO 检测，需要人工/半自动标注 bbox 并导出 YOLO `.txt` 标签。

## 10. 后续正式训练建议

- 若以分类识别为主：继续使用当前分类数据集，训练更多 epoch，做混淆矩阵、Top-1/Top-5、每类准确率和推理延迟测试。
- 若以目标检测为主：使用 LabelImg、CVAT、Label Studio 或 Roboflow 对每张图进行 bbox 标注，导出 YOLO `.txt` 标签后再训练检测模型。
- 若时间不足但必须提交检测链路：使用整图框弱标注数据跑通 YOLO 检测流程，并在报告中清楚标注“弱标注，不代表真实定位能力”。

## 11. 2026-06-07 最新权威状态补充

截至 2026-06-07 06:27，本项目已经从早期 1 epoch 链路验证推进到 strict 92 类 5 epoch 分类基准、93 类补齐冒烟、重复/泄漏审计、候选清理集、跨类别复核、人工决策表、清理计划和执行入口阶段。

当前关键状态：

- strict 92 类分类基准：`runs/train/strict_classify_yolo12n_cls_cpu_5e`，Top-1 0.1769，Top-5 0.4957。
- 93 类补齐冒烟：`data/classification_strict_jpeg_93class`，`大腹皮` 由 val-only 样本确定性重划为 15 train / 4 val；1 epoch CPU Top-1 0.0665，Top-5 0.2086。
- train/val exact duplicate 已通过候选清理集移除：strict 候选集 9533 张、93 类候选集 9552 张，两者 train/val 泄漏组均为 0。
- 剩余主要数据风险：候选清理集仍有 34 组跨类别 exact duplicate，已整理到 `reports/classification_cross_class_review_package.*` 和 `reports/figures/classification_cross_class_contact_sheet.png`。
- 人工决策入口：`reports/classification_cross_class_decision_table.csv`，当前 34 / 34 组仍为 `pending`。
- 清理计划：`reports/classification_cross_class_cleanup_plan.md/json/csv`，当前状态 `waiting_for_manual_decisions`，计划操作 0。
- 清理执行入口已存在：`scripts/apply_classification_cross_class_cleanup_plan.py`；计划未达到 `ready_for_cleanup_execution` 时应阻断，真实执行也只写入新的输出目录。
- 检测 bbox 标注仍未完成：`data/detection_annotation_package` 覆盖 93 类、279 张待标注图片，`labels/` 仍为空，当前不能汇报真实检测 mAP、Precision、Recall 或 bbox 定位能力。
- 最新可复现 Manifest：`reports/repro_manifest.md/json`，92 个关键产物 SHA256，31 条复现命令。
- 最新增强工程自检：`reports/smoke_check.md/json`，157 / 157 通过，已验证当前 pending 清理计划会被 `apply_classification_cross_class_cleanup_plan.py` 阻断，且不产生清理输出目录或执行报告。

当前阻断点不是缺少清理执行脚本，而是人工 CSV 未填写。下一步应先基于 `classification_cross_class_contact_sheet.png` 逐组填写 `classification_cross_class_decision_table.csv` 的真实类别、动作、复核人和说明；之后重新生成 `classification_cross_class_cleanup_plan.*`，确认状态为 `ready_for_cleanup_execution` 且校验错误为 0，再运行清理执行入口重建数据集、重新审计、重新训练和评估。
